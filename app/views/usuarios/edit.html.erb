<%= form_for(@usuario) do |form| %>
	<%= form.file_field :my_photos %>
	<%= form.file_field :my_cover %>
	<%= form.text_field :username %>
	<%= form.submit "editar" %>
<% end %>

<%= @usuario.username %>
